@model SimpleDBContext.Models.Employee

@{
    ViewBag.Title = "Create";
}

<h1>添加员工</h1>
@using (Html.BeginForm())
{
    <div>
        @Html.LabelFor(m => m.Name)
        @Html.EditorFor(m => m.Name)
        @Html.ValidationMessageFor(m => m.Name)
    </div>

    <div>
        <input type="submit" value="保存"/>
    </div>
}